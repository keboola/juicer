languages:
  PHP: true
  exclude_paths:
  - "vendor"
engines:
  duplication:
    enabled: true
    config:
      languages:
      - php
    exclude_paths:
    - "Tests/"
  fixme:
    enabled: true
  phpmd:
    enabled: true
    config:
      rulesets: "cleancode,codesize,controversial,design,naming,unusedcode,juicer-md.xml"    
