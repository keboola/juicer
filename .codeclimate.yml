languages:
  PHP: true
  exclude_paths:
  - "vendor"
engines:
  duplication:
    enabled: true
    config:
      languages:
      - php
    exclude_paths:
    - "Tests/"
  fixme:
    enabled: true
  phpmd:
    enabled: false
    rulesets: "unusedcode"
