keboola_{{ appName }}_extractor_configs_list:
    pattern:  /configs
    defaults: { _controller: Keboola{{ classPrefix }}ExtractorBundle:Configs:get }
    requirements:
        _method:  GET

keboola_{{ appName }}_extractor_configs_add:
    pattern:  /configs
    defaults: { _controller: Keboola{{ classPrefix }}ExtractorBundle:Configs:post }
    requirements:
        _method:  POST

keboola_{{ appName }}_extractor_configs_delete:
    pattern:  /configs/{id}
    defaults: { _controller: Keboola{{ classPrefix }}ExtractorBundle:Configs:delete }
    requirements:
        _method:  DELETE

keboola_{{ appName }}_extractor_configs_getconfig:
    pattern:  /configs/{id}
    defaults: { _controller: Keboola{{ classPrefix }}ExtractorBundle:Configs:getConfig }
    requirements:
        _method:  GET

keboola_{{ appName }}_extractor_configs_addrow:
    pattern:  /configs/{id}/rows
    defaults: { _controller: Keboola{{ classPrefix }}ExtractorBundle:Configs:addRow }
    requirements:
        _method:  POST

keboola_{{ appName }}_extractor_configs_addrowid:
    pattern:  /configs/{id}/rows/{rowId}
    defaults: { _controller: Keboola{{ classPrefix }}ExtractorBundle:Configs:addRow }
    requirements:
        _method:  POST

keboola_{{ appName }}_extractor_configs_getrow:
    pattern:  /configs/{id}/rows/{rowId}
    defaults: { _controller: Keboola{{ classPrefix }}ExtractorBundle:Configs:getRow }
    requirements:
        _method:  GET

keboola_{{ appName }}_extractor_configs_deleterow:
    pattern:  /configs/{id}/rows/{rowId}
    defaults: { _controller: Keboola{{ classPrefix }}ExtractorBundle:Configs:deleteRow }
    requirements:
        _method:  DELETE

keboola_{{ appName }}_extractor_configs_updaterow:
    pattern:  /configs/{id}/rows/{rowId}
    defaults: { _controller: Keboola{{ classPrefix }}ExtractorBundle:Configs:addRow }
    requirements:
        _method:  PUT

keboola_{{ appName }}_extractor_configs_addcreds:
    pattern:  /configs/{id}/credentials
    defaults: { _controller: Keboola{{ classPrefix }}ExtractorBundle:Configs:addAttrs }
    requirements:
        _method:  POST


