language: bash

services:
  - docker

before_script:
  - docker -v
  - docker-compose -v
  - docker-compose build

script:
  - docker-compose run --rm -e CODECLIMATE_REPO_TOKEN tests

notifications:
  slack: $SLACK_TOKEN    
  email: false
